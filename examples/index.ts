// 1

const message = "Hello world";
const newMessage = message.toLocaleLowerCase();
newMessage();

// 2

const shape = {
  width: 100,
  height: 200,
};

const area = shape.width * shape.heigth;

// 3

let username = "Andrey";
if (username === "Andrey") {
  username = 10;
}
